import { Dashboard } from "@hashtagcms/admin-sdk";

export const {
  data,
  labelsCategories,
  datasCategories,
  labelsContent,
  datasContent,
  bgColors,
  borderColors,
  createChart,
  makeData,
  init
} = Dashboard;

export { Dashboard };
