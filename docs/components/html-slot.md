# HtmlSlot Component

**Package:** `@hashtagcms/admin-ui-kit`
**Source:** `themes/modern/components/html-slot.vue`

The `HtmlSlot` is a simple wrapper component that renders its default slot content. It serves as a container for injecting raw HTML or other Vue components dynamically.

## 🚀 Import

```javascript
import { HtmlSlot } from "@hashtagcms/admin-ui-kit";
```

## 📝 Usage Example

```html
<html-slot>
  <p>This is dynamic content.</p>
</html-slot>
```
